# Music Library Manager Dependencies

# Core dependencies
pandas>=1.3.0          # Data manipulation and display
ipython>=7.0.0         # Rich display in Jupyter notebooks
jupyter>=1.0.0         # Jupyter notebook environment

# Phase 1 & 2 & 4 dependencies (implemented)
mutagen>=1.45.0        # Audio metadata (ID3 tags) manipulation and audio analysis

# Phase 6 dependencies (visualizations - implemented)
matplotlib>=3.3.0      # Static visualizations
seaborn>=0.11.0        # Enhanced statistical plots
numpy>=1.20.0          # Numerical operations for charts

# Phase 6 optional (interactive visualizations)
# plotly>=5.0.0        # Interactive dashboards (install separately if needed)

# Phase 3 dependencies (implemented)
Pillow>=8.0.0          # Image processing for album art (Module 03)
musicbrainzngs>=0.7.1  # MusicBrainz API (Module 05)
requests>=2.25.0       # HTTP requests for API calls and downloads (Module 05)

# Optional dependencies for future enhancements
# audioread>=2.1.9     # Audio file validation (future)
